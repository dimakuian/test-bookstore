# Application name
spring.application.name=test-bookstore

# DataSource, Hibernate and JPA configuration
spring.jpa.hibernate.ddl-auto=none
spring.sql.init.mode=always
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.show-sql=true
spring.datasource.url=jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/${MYSQL_DATABASE:bookstoredb}
spring.datasource.username=${MYSQL_USER:root}
spring.datasource.password=${MYSQL_PASSWORD:root}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# External API configuration for currency conversion
fixer.api.url=${fixer.api.base-url}?access_key=${fixer.api.key}&base=${fixer.api.base-currency}&symbols=${fixer.api.symbols}
fixer.api.key=ef40e2a95159458bf1585785ca97cf15
fixer.api.base-url=http://data.fixer.io/api/latest
fixer.api.base-currency=EUR
fixer.api.symbols=USD,UAH

# JWT (JSON Web Token) configuration
jwt.secret=GcGP6faXWDyg7FeGkShNM9gjtFtkt8Zh;
jwt.expiration-date=3600000